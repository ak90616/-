# Step 2｜Wireframe、內容注入、視覺方向

工具：Claude Code（接 Figma MCP）、Figma、Google Stitch

這一步的心法是：AI 出方向，你出成稿。所有 AI 生的視覺都只是參考，最終的間距、字級、佈局還是自己在 Figma 完成

## 2-1 整合兩個 wireframe 方案

先讓 AI 出幾個佈局方向，挑出你要的部分，再讓它合併。重點是把「你喜歡哪一個的哪一點」講清楚，不要只說「幫我合併」

```
讀取這兩個 frame：
<方案 A 的 Figma frame 連結>
<方案 C 的 Figma frame 連結>

我最喜歡 A 的一屏敘事，每一屏講一件事、往下捲就是往下講；
但我也想要 C 的固定說明，產品釘在畫面上、文字依序進場。

請結合兩個版本的優點，給我一版整合的 wireframe：
以 A 的一屏敘事為骨架，加上 C 的常駐文字說明。
先告訴我你打算怎麼整合、每一屏對應什麼內容，我確認後再畫。
```

訣竅就是不斷要求它照你的想法調整，直到滿意為止

## 2-2 把真實內容注入 wireframe

Wireframe 裡都是假的替代文字。既然官方資訊已經蒐集好了，讓 AI 一次填完，不用手動一格一格貼

```
這是我從官方網站蒸餾出來的產品內容：
<貼上內容，或給檔案路徑>

請把這些內容注入到這個 frame 的對應欄位，取代現有的替代文字：
<Figma frame 連結>

規則：
- 只換文字，不動任何佈局、間距、字級
- 找不到對應內容的欄位保持原樣，不要自己編
- 換完列一張表告訴我哪些欄位換了、哪些沒換
```

「找不到對應的不要自己編」這條一定要寫。少了它，AI 會很自然地幫你捏造規格數字

## 2-3 讓 Claude 幫你寫給別的 AI 工具的 prompt

Prompt 不用全部自己寫。要餵給 Google Stitch 這類視覺生成工具的時候，讓讀過你 PRD 和 wireframe 的 Claude 來寫，會比你自己憑空想準得多

```
讀取這個 wireframe：<Figma frame 連結>
結合我們之前生成的 PRD，幫我寫一個完整的 prompt 指令，給其他 AI 工具生成 UI 視覺用。

這個 prompt 要能讓對方基於這份 wireframe 的結構、以及我 mood board 的風格 DNA，
產出幾個不同的視覺方向。請直接給我可以整段複製的 prompt 正文。
```

這裡有個副作用值得注意：Claude 會自己去翻檔案，把你最後那版 mood board 找出來。所以 **Figma 裡的圖層和 frame 命名非常重要**，因為 AI 是真的會去讀

## 2-4 丟給 Google Stitch

把上一步產出的 prompt，連同 wireframe 和 mood board 的截圖一起丟進去

模式選 **Ideate**，先讓它發想，不要直接叫它做設計。它會先基於你的風格 DNA 和 PRD 給出三個不同方向的提案，你判斷要走哪一種風格，再讓它基於那份提案生成 UI 視覺

過程中它會出錯，例如某些介面沒有完全生成。透過對話要求它調整就好，把它當成設計版本的 Claude Code 用

AI 的作用是幫助你確定方向，把試錯的成本降到最低。但這些全部都只是方向，如果你是設計師，最終成稿建議還是在 Figma 裡自己完成
